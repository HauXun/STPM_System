import { connect } from 'react-redux';
import TopicAttachmentInfo from '../components/TopicAttachmentInfo';

const mapStateToProps = (state: unknown) => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(TopicAttachmentInfo);
